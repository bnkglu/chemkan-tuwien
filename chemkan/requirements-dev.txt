# Dev + notebook tooling. Installs the full runtime lock first, then adds test/lint
# and notebook dependencies. Equivalent to: pip install -e ".[dev,notebooks]".
-r requirements.txt

# tests + lint
pytest
ruff

# notebooks (einsum + from-scratch walkthroughs, data-generation walkthrough)
matplotlib==3.8.4
jupyter
nbconvert
ipykernel
